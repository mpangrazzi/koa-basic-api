
/**
 * Log
 */

var L = require('npmlog');

var env = process.env.NODE_ENV || 'development';

L.enableColor();
L.addLevel('boot', 6000, { fg: 'green' });

L.level = env !== 'production' ? 'verbose' : 'info';
L.stream = process.stdout;

module.exports = L;
