
/**
 * Module dependencies
 */

var log = require('npmlog');

var env = process.env.NODE_ENV || 'development';

log.enableColor();
log.addLevel('boot', 6000, { fg: 'green' });

log.level = env !== 'production' ? 'verbose' : 'info';
log.stream = process.stdout;

module.exports = log;